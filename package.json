{
  "name": "renabile-server",
  "version": "5.5.0",
  "description": "Core MESH server for RenaBile Messenger",
  "main": "storage-server.js",
  "scripts": {
    "start": "node storage-server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
